{
  "name": "@oagi/monorepo",
  "private": true,
  "workspaces": [
    "sdk",
    "examples"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "prepare": "husky"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "husky": "^9.0.0"
  }
}